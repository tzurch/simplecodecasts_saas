<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <% if params[:plan] == '2' %>
      <h1>Pro Account</h1>
      <p>Sign up for the Pro account for only $10/month and get full access to the many experts in the community to kick start your business! Stop at anytime.</p>
    <% else %>
      <h1>Basic Account</h1>
      <p>Sign up for FREE and get started with basic access to the community with no obligation.</p>
    <% end %>
  </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
    <h2>Sign up</h2>
      
        <% if params[:plan] == '2' %>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>
        
        <%= render "devise/shared/links" %>
    </div>
  </div>
</div>